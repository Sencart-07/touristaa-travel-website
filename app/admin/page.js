'use client';
import {useEffect,useState} from 'react';
export default function Admin(){const [packages,setPackages]=useState([]),[enquiries,setEnquiries]=useState([]),[loading,setLoading]=useState(true),[form,setForm]=useState({title:'',destination:'',price:'',duration:'',description:''});
async function load(){setLoading(true);try{const p=await fetch('/api/packages').then(r=>r.json());const e=await fetch('/api/enquiries').then(r=>r.json());setPackages(p.packages||[]);setEnquiries(e.enquiries||[])}catch{}finally{setLoading(false)}}
useEffect(()=>{load()},[]);
async function addPackage(ev){ev.preventDefault();const r=await fetch('/api/packages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(r.ok){setForm({title:'',destination:'',price:'',duration:'',description:''});load()}}
return <div className="admin"><div className="adminNav"><img src="/touristaa-logo.svg" alt="Touristaa Travel Company" className="adminLogo"/><a href="/">← View Website</a></div><div className="dashboard"><div className="eyebrow">Admin Dashboard</div><h1>Touristaa Management</h1><p className="muted">Manage tour packages and customer enquiries.</p><div className="dashCards"><div className="dashCard">Total Packages<strong>{packages.length}</strong></div><div className="dashCard">Enquiries<strong>{enquiries.length}</strong></div><div className="dashCard">Active Tours<strong>{packages.length}</strong></div><div className="dashCard">Website<strong>Live</strong></div></div><div className="adminGrid"><section className="table"><h2>Add Tour Package</h2><form onSubmit={addPackage} className="adminForm"><input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Package name"/><input value={form.destination} onChange={e=>setForm({...form,destination:e.target.value})} placeholder="Destination"/><input value={form.price} onChange={e=>setForm({...form,price:e.target.value})} placeholder="Price"/><input value={form.duration} onChange={e=>setForm({...form,duration:e.target.value})} placeholder="Duration"/><textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Description"/><button className="btn primary" type="submit">Add Package</button></form></section><section className="table"><h2>Tour Packages</h2>{loading?<p>Loading...</p>:packages.length===0?<p>No packages yet.</p>:packages.map(p=><div className="tableRow" key={p._id}><div><strong>{p.title||p.name}</strong><br/><span className="small">{p.destination}</span></div><div>{p.price||'Custom Quote'}</div><div><span className="badge">Active</span></div></div>)}</section></div><section className="table"><h2>Customer Enquiries</h2>{loading?<p>Loading...</p>:enquiries.length===0?<p>No enquiries yet.</p>:enquiries.map(e=><div className="tableRow" key={e._id}><div><strong>{e.name}</strong><br/><span className="small">{e.destination||'General enquiry'} · {e.phone}</span></div><div>{e.email||''}</div><div><span className="badge">New</span></div><div>{new Date(e.createdAt).toLocaleDateString()}</div></div>)}</section></div></div>}
