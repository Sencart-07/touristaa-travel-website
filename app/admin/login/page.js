'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function Login(){const router=useRouter();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await r.json();if(!r.ok){setError(data.error||'Login failed');return}router.push('/admin')}finally{setBusy(false)}}return <main className="loginPage"><div className="loginCard"><img src="/touristaa-logo.svg" alt="Touristaa" className="loginLogo"/><div className="eyebrow">Secure Area</div><h1>Admin Login</h1><p>Sign in to manage Touristaa packages and enquiries.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="admin@example.com"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="••••••••"/></label>{error&&<div className="loginError">{error}</div>}<button className="btn primary fullBtn" disabled={busy}>{busy?'Signing in...':'Sign In'}</button></form><a href="/">← Back to website</a></div></main>}
